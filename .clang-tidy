# AGENTS.md §1 — naming and semantics, machine-readable. This file is the single source of truth
# for the option values; AGENTS.md states the rules in prose and does not repeat the settings.
#
# The values below are adopted from the sibling repository Outpost.Warzone so that engine code
# moves between the two trees without a rename pass.
#
# This file owns IDENTIFIERS and semantics. /.clang-format owns whitespace, and the two never
# overlap: clang-tidy has no check that reports a wrong brace position or an over-long line, and
# clang-format cannot see that a parameter is missing its `_`.
#
# ── Status: CI gates this over GameLogic and NeuronServer ──────────────────────────────────
#
# Both linters gate (AGENTS.md §6): clang-format over the whole tree on Linux, clang-tidy on
# Windows after the build, over GameLogic and NeuronServer -- the second promoted on 2026-08-30
# after two clean runs under the runner's LLVM 22, which is how a linter arrives here and starts
# gating. The .vcxproj files still do not enable MSBuild code
# analysis, so on the other seven projects this file is enforced by review and by whoever remembers
# to run it. Slice 23 of Design/Archive/MmoScalabilityPlan.md widens the job a project at a time; NeuronCore
# is next, and slice 22 already swept its one header.
# Run it by hand ON THE FILES YOU WROTE -- not on the tree, where the engine projects reach D3D12
# and C++/WinRT headers that have never been swept and will bury your own findings:
#
#   clang-tidy --quiet NeuronCore/YourNewFile.cpp -- -I . -D _WIN32 -D _DEBUG /std:c++latest
#
# GameLogic has been swept clean under clang-tidy 18 (Linux) and LLVM 20.1.8 (the CI runner),
# so a finding there is new work rather than archaeology. What that cost: two identifier
# findings (local constexpr constants, a lambda parameter), one truncating division split out
# of the float expression it fed so the intent reads, the `-performance-enum-size` exclusion
# below, and -- from the Windows run only, which is the argument for having it -- a `noexcept`
# on `ComputeUniverseHash` that its own per-list allocation could not keep.
#
# NeuronServer was swept under clang-tidy 18 (Linux) before the job was widened to it and came back
# with nothing at all -- three headers and one translation unit, which is why it was the project to
# take next. The runner's LLVM 22 then found it clean on two consecutive main runs (2026-08-30),
# and it gates.
#
# Unlike the sibling tree there is nothing grandfathered here — this is a greenfield repository,
# so a whole-tree pass should come back clean and a finding is a defect, not archaeology. That
# claim has now been tested on two projects; the graphics-facing ones are still untested, and the
# version spread is itself a finding: the newer clang-tidy saw a defect the older one did not, so
# "clean" is always clean *under something*.

# Three rules of AGENTS.md §1 this file cannot express — R2's banned type affixes, R7's file naming
# and .vcxproj/.filters registration, and R11's standing spelling families — are checked by
# Build/CheckProjectFiles.py, which runs in the same CI job that builds. A check that exists
# somewhere is worth more than a rule everyone is asked to remember.
#
# readability-identifier-length is deliberately NOT enabled. Its defaults reject the domain
# vocabulary this codebase is built on — x, y, _a, _b — and short names here are precise, not lazy.

Checks: >
  readability-identifier-naming,
  bugprone-*,
  performance-*,
  modernize-use-nullptr,
  modernize-use-override,
  -bugprone-easily-swappable-parameters,
  -bugprone-narrowing-conversions,
  -clang-analyzer-optin.performance.Padding,
  -performance-enum-size

# `-clang-analyzer-optin.performance.Padding` is a judgement call, not a mechanical exclusion. It
# is an OPT-IN analyzer that arrives with clang-tidy's defaults because this list does not begin
# with `-*`. Wire records, mesh vertices and GPU instance structs have layouts that are
# load-bearing — a snapshot record is the size the wire says it is — and
# reordering fields to save padding is exactly the change this repository must not make casually.
#
# `-bugprone-narrowing-conversions` is off because quantisation is pervasive and deliberate:
# metres to eighth-metre lattice steps, radians to turns16, i64 sectors to the wire's i32, float to
# the integer wire types. It would fire on most of the serialization layer, where the narrowing IS
# the design. This block described that layer for a year before there was one and said centimetres
# when the tree does eighth-metres; both are now true rather than aspirational (ADR 0042).
#
# `-performance-enum-size` is off for the same reason as the padding check: it asks a closed
# taxonomy to shrink to the smallest type its current enumerators happen to fit in. Every such
# taxonomy in this tree is a wire value first and a C++ type second, so "no enumerator needs that
# many bits yet" is an argument for renumbering a protocol, which is the change this repository
# must not make casually.
#
# NOTE ON THE FORMAT: `Checks:` is a FOLDED scalar, so a `#` line indented under it is not a
# comment — it folds into the value and corrupts the check list. Every comment belongs here.
#
# WarningsAsErrors: '*' says every enabled check is fatal, so that a future gate has one answer
# rather than two settings disagreeing about which findings matter.
WarningsAsErrors: '*'

# [/\\], not '/'. On a Windows Clang build the paths arrive with backslashes, so a filter anchored
# on a forward slash matches no header at all and silently suppresses every in-build diagnostic.
# Headers sitting DIRECTLY in a project folder, which is every hand-written one (AGENTS.md 3's
# flat-directory rule). The `.*` this replaced also matched build output -- the C++/WinRT
# projection under <Project>/x64/Debug/Generated Files/winrt/ -- and 46 of the 77 diagnostics in
# the first sweep were from headers no one in this repository wrote.
HeaderFilterRegex: '(NeuronCore|GameLogic|NeuronServer|NeuronClient|Outpost)[/\\][A-Za-z0-9]+\.h$'

CheckOptions:
  # ── Types: PascalCase, no prefix of any kind (R2, R5) ──────────────────────────────
  - { key: readability-identifier-naming.NamespaceCase,            value: CamelCase  }
  - { key: readability-identifier-naming.ClassCase,                value: CamelCase  }
  - { key: readability-identifier-naming.StructCase,               value: CamelCase  }
  - { key: readability-identifier-naming.EnumCase,                 value: CamelCase  }
  - { key: readability-identifier-naming.EnumConstantCase,         value: CamelCase  }
  - { key: readability-identifier-naming.TypeAliasCase,            value: CamelCase  }
  - { key: readability-identifier-naming.TypedefCase,              value: CamelCase  }
  - { key: readability-identifier-naming.ConceptCase,              value: CamelCase  }
  - { key: readability-identifier-naming.TemplateParameterCase,    value: CamelCase  }
  - { key: readability-identifier-naming.AbstractClassPrefix,      value: ''         }

  # ── Functions: PascalCase ──────────────────────────────────────────────────────────
  - { key: readability-identifier-naming.FunctionCase,             value: CamelCase  }
  - { key: readability-identifier-naming.MethodCase,               value: CamelCase  }

  # ── Constants: UPPER_CASE (R3); enumerators stay PascalCase above ──────────────────
  - { key: readability-identifier-naming.ConstexprVariableCase,    value: UPPER_CASE }
  - { key: readability-identifier-naming.GlobalConstantCase,       value: UPPER_CASE }
  - { key: readability-identifier-naming.ClassConstantCase,        value: UPPER_CASE }
  - { key: readability-identifier-naming.StaticConstantCase,       value: UPPER_CASE }

  # ── Storage prefixes ───────────────────────────────────────────────────────────────
  - { key: readability-identifier-naming.PrivateMemberCase,        value: camelBack  }
  - { key: readability-identifier-naming.PrivateMemberPrefix,      value: 'm_'       }
  - { key: readability-identifier-naming.ProtectedMemberCase,      value: camelBack  }
  - { key: readability-identifier-naming.ProtectedMemberPrefix,    value: 'm_'       }
  - { key: readability-identifier-naming.ClassMemberCase,          value: camelBack  }
  - { key: readability-identifier-naming.ClassMemberPrefix,        value: 'sm_'      }
  - { key: readability-identifier-naming.GlobalVariableCase,       value: camelBack  }
  - { key: readability-identifier-naming.GlobalVariablePrefix,     value: 'g_'       }
  - { key: readability-identifier-naming.StaticVariableCase,       value: camelBack  }
  - { key: readability-identifier-naming.StaticVariablePrefix,     value: 'g_'       }

  # R8: a public aggregate uses plain camelCase fields so brace initialization reads naturally.
  # m_ marks encapsulated state, not every field — wire records and Desc structs carry no prefix.
  - { key: readability-identifier-naming.PublicMemberCase,         value: camelBack  }

  # ── Parameters carry a leading underscore (R1) ─────────────────────────────────────
  # Legal C++: the reserved forms are _Uppercase, anything containing __, and _lowercase at
  # GLOBAL scope. A parameter is never at global scope.
  - { key: readability-identifier-naming.ParameterCase,            value: camelBack  }
  - { key: readability-identifier-naming.ParameterPrefix,          value: '_'        }
  - { key: readability-identifier-naming.LocalVariableCase,        value: camelBack  }

  - { key: readability-identifier-naming.MacroDefinitionCase,      value: UPPER_CASE }
